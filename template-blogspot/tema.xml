<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:version='2' class='v2' expr:dir='data:blog.languageDirection'
  xmlns='http://www.w3.org/1999/xhtml'
  xmlns:b='http://www.google.com/2005/gml/b'
  xmlns:data='http://www.google.com/2005/gml/data'
  xmlns:expr='http://www.google.com/2005/gml/expr'>

<head>
  <meta charset='UTF-8'/>
  <meta content='width=device-width, initial-scale=1.0' name='viewport'/>
  <b:include data='blog' name='all-head-content'/>

  <script type='application/ld+json'>
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "Didi Index Setup",
    "url": "https://bigplayer.vercel.app/",
    "logo": "https://bigplayer.vercel.app/favicon.svg"
  }
  </script>
  <b:if cond='data:blog.pageType == &quot;index&quot;'>
    <script type='application/ld+json'>
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "Agulhada do Didi",
      "url": "https://agulhadadodidi.blogspot.com/"
    }
    </script>
  </b:if>

  <link rel='preconnect' href='https://fonts.googleapis.com'/>
  <link rel='preconnect' href='https://fonts.gstatic.com' crossorigin='crossorigin'/>
  <link href='https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&amp;display=swap' rel='stylesheet'/>
  <b:skin><![CDATA[

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg: #F5F3EE;
  --ink: #1A1917;
  --ink2: #6B6860;
  --ink3: #A8A59E;
  --box: #E8E5DE;
  --border: #C8C4BB;
  --white: #FFFFFF;
  --yellow: #F5E642;
  --r: 4px;
  --font: 'DM Sans', system-ui, sans-serif;
}

body {
  font-family: var(--font);
  background: var(--bg);
  color: var(--ink);
  font-size: 17px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
a { color: inherit; }
img { max-width: 100%; height: auto; }

.container { max-width: 960px; margin: 0 auto; padding: 0 32px; }
.narrow { max-width: 760px; margin: 0 auto; padding: 0 32px; }

/* ESCONDE LIXO PADRAO DO BLOGGER */
#header, .header-outer, .Header { display: none !important; }
.date-header, h2.date-header { display: none !important; }
.post-footer, .post-footer-line, .post-header { display: none !important; }
.comments, #comments, .post-comment-link { display: none !important; }
.feed-links, .post-feeds { display: none !important; }
.widget-item-control, .quickedit, .home-link { display: none !important; }
.navbar, #navbar, #Navbar1 { display: none !important; height: 0 !important; visibility: hidden !important; }
.post-share-buttons, .post-labels { display: none !important; }
.byline.reactions { display: none !important; }

/* NAV PRETA */
#nav-bar {
  background: #000;
  border-bottom: 1px solid #2a2a2a;
  position: sticky;
  top: 0;
  z-index: 100;
}
#nav-bar .container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 56px;
  max-width: none;
  padding: 0 32px;
}
.nav-logo {
  font-size: 15px;
  font-weight: 700;
  color: #fff;
  text-decoration: none;
  letter-spacing: .02em;
}
.nav-right { display: flex; align-items: center; gap: 20px; margin-left: auto; }
.nav-link {
  font-size: 13px;
  color: #A0A0A0;
  text-decoration: none;
  font-weight: 500;
}
.nav-link:hover { color: #fff; }
.nav-cta {
  font-size: 13px;
  font-weight: 700;
  background: #fff;
  color: #000;
  padding: 8px 16px;
  border-radius: var(--r);
  text-decoration: none;
  letter-spacing: .03em;
  white-space: nowrap;
}
.nav-cta:hover { opacity: .85; }

/* HERO ESCURO */
.hero {
  background: #1A1917;
  color: #fff;
  padding: 88px 0 80px;
  border-bottom: 1px solid #2a2a2a;
}
.hero-tag {
  display: inline-block;
  font-size: 10px;
  letter-spacing: .12em;
  text-transform: uppercase;
  border: 1px solid #3a3a38;
  padding: 5px 11px;
  border-radius: 2px;
  color: #A8A59E;
  margin-bottom: 22px;
}
.hero h1 {
  font-size: clamp(32px, 5vw, 46px);
  font-weight: 700;
  line-height: 1.15;
  letter-spacing: -.02em;
  margin-bottom: 20px;
  max-width: 720px;
}
.hero h1 .hl {
  background: #fff;
  color: #000;
  padding: 0 7px;
}
.hero-sub {
  font-size: 17px;
  color: #C8C4BB;
  line-height: 1.7;
  max-width: 560px;
  margin-bottom: 30px;
}
.hero-cta { display: flex; align-items: center; gap: 16px; flex-wrap: wrap; }
.btn-primary {
  display: inline-flex;
  align-items: center;
  font-size: 14px;
  font-weight: 700;
  background: var(--yellow);
  color: #000;
  padding: 15px 26px;
  border-radius: var(--r);
  text-decoration: none;
  letter-spacing: .02em;
  transition: transform .15s;
}
.btn-primary:hover { transform: translateY(-2px); }
.hero-trust {
  display: flex;
  gap: 22px;
  flex-wrap: wrap;
  margin-top: 30px;
}
.trust-item {
  font-size: 10px;
  letter-spacing: .05em;
  text-transform: uppercase;
  color: #A8A59E;
  display: flex;
  align-items: center;
  gap: 6px;
}
.trust-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--yellow); }

/* BARRA DE TITULO (paginas de post) */
.page-band {
  background: #1A1917;
  color: #fff;
  padding: 36px 0;
  border-bottom: 1px solid #2a2a2a;
}
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  letter-spacing: .04em;
  flex-wrap: wrap;
}
.breadcrumb a {
  color: #A8A59E;
  text-decoration: none;
}
.breadcrumb a:hover { color: #fff; }
.bc-sep { color: #555; font-size: 10px; }
.bc-current { color: #fff; }

/* DATA DO POST */
body.item .post-header {
  display: block !important;
  margin-bottom: 4px;
}
body.item .post-header .published,
body.item .published,
body.item .timestamp-link {
  font-size: 12px;
  color: var(--ink3);
  letter-spacing: .04em;
  text-transform: uppercase;
}
body.item .post-header .author,
body.item .post-header .reaction-buttons,
body.item .post-header-line-1 { display: none !important; }

/* SECAO POSTS RELACIONADOS */
.related-band {
  padding: 40px 0 8px;
  border-top: 1px solid var(--border);
  background: var(--box);
}
.related-band .sec-label { margin-bottom: 22px; }
.related-band .widget h2,
.related-band .widget h3,
.related-band .widget .title,
.related-band .widget .widget-title,
#PopularPosts1 h2,
#PopularPosts1 h3,
#PopularPosts1 .title { display: none !important; }
.popular-posts li:nth-child(n+7) { display: none; }
.popular-posts ul {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
}
.popular-posts li {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--r);
  padding: 24px 26px !important;
  transition: transform .15s, box-shadow .15s;
}
.popular-posts .item-content,
.popular-posts .item-title {
  padding: 0 !important;
  margin: 0 !important;
}
.popular-posts li:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 14px rgba(0,0,0,.06);
}
.popular-posts li a {
  font-size: 14px;
  font-weight: 700;
  color: var(--ink);
  text-decoration: none;
  line-height: 1.4;
  display: block;
}
.popular-posts li::after {
  content: 'Ler artigo \2192';
  font-size: 10px;
  font-weight: 600;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: var(--ink3);
  margin-top: 16px;
}
.popular-posts .item-thumbnail,
.popular-posts .item-snippet,
.popular-posts .item-snippet-link { display: none; }
.popular-posts .item-title { margin: 0; }
@media (max-width: 760px) {
  .popular-posts ul { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 480px) {
  .popular-posts ul { grid-template-columns: 1fr; }
}

/* SECTION LABEL NUMERADO */
.sec-label {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 10px;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--ink3);
  margin-bottom: 28px;
}
.sec-num {
  font-size: 10px;
  background: var(--ink);
  color: var(--bg);
  padding: 2px 7px;
  border-radius: 2px;
}
.sec-label::after {
  content: '';
  flex: 1;
  height: 1px;
  background: var(--border);
}

/* AREA DOS POSTS */
.main-outer { padding: 64px 0 24px; }

.post-outer, .post-outer-container {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--r);
  padding: 32px;
  margin-bottom: 20px;
}

.post-title, h3.post-title, h1.post-title {
  font-size: 23px;
  font-weight: 700;
  line-height: 1.3;
  margin-bottom: 16px;
  letter-spacing: -.01em;
}
.post-title a { color: var(--ink); text-decoration: none; }
.post-title a:hover { color: var(--ink2); }

.post-body, .post-body.entry-content {
  font-size: 16px;
  line-height: 1.75;
  color: var(--ink);
}
.post-body p { margin-bottom: 14px; }
.post-body h2 { font-size: 20px; font-weight: 700; margin: 28px 0 10px; }
.post-body h3 { font-size: 17px; font-weight: 700; margin: 22px 0 8px; }
.post-body ul, .post-body ol { margin: 0 0 14px 22px; }
.post-body li { margin-bottom: 4px; }
.post-body strong { font-weight: 700; }
.post-body a { color: var(--ink); font-weight: 600; text-decoration: underline; }
.post-body img { border-radius: var(--r); border: 1px solid var(--border); margin: 18px 0; }

/* INDEX: grid de 3 cards pequenos */
body.index .blog-posts {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
body.index .date-outer,
body.index .date-posts { display: contents; }
body.index .post-outer,
body.index .post-outer-container {
  margin: 0;
  padding: 22px;
  display: flex;
  flex-direction: column;
  transition: transform .15s, box-shadow .15s;
}
body.index .post-outer:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 18px rgba(0,0,0,.08);
}
body.index .post-title,
body.index h3.post-title {
  font-size: 15px;
  line-height: 1.4;
  margin-bottom: 0;
}
body.index .post-body,
body.index .post-jump-link,
body.index .jump-link { display: none; }
body.index .post-outer::after {
  content: 'Ler artigo \2192';
  font-size: 11px;
  font-weight: 600;
  letter-spacing: .04em;
  text-transform: uppercase;
  color: var(--ink3);
  margin-top: auto;
  padding-top: 16px;
}

/* ITEM: artigo unico centralizado */
body.item .post-outer,
body.item .post-outer-container {
  max-width: 760px;
  margin: 0 auto 20px;
}

@media (max-width: 760px) {
  body.index .blog-posts { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 480px) {
  body.index .blog-posts { grid-template-columns: 1fr; }
}

/* PAGINACAO (esconde nas paginas de post - Continue lendo cobre) */
body.item .blog-pager,
body.item #blog-pager { display: none !important; }

.blog-pager, #blog-pager {
  display: flex;
  justify-content: center;
  gap: 12px;
  flex-wrap: wrap;
  padding: 16px 0 0;
}
.blog-pager a, #blog-pager a {
  font-size: 13px;
  font-weight: 600;
  color: var(--ink2);
  text-decoration: none;
  border: 1px solid var(--border);
  padding: 9px 18px;
  border-radius: var(--r);
  background: var(--white);
}
.blog-pager a:hover, #blog-pager a:hover { border-color: var(--ink); color: var(--ink); }

/* BANNER CTA */
.cta-band { padding: 40px 0 72px; }
.cta-box {
  background: var(--ink);
  color: var(--bg);
  border-radius: var(--r);
  padding: 48px 32px;
  text-align: center;
}
.cta-box .kicker {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--ink3);
  display: block;
  margin-bottom: 14px;
}
.cta-box h2 { font-size: 25px; font-weight: 700; line-height: 1.25; margin-bottom: 10px; }
.cta-box p { font-size: 14px; color: #C8C4BB; margin-bottom: 4px; }
.cta-box .price { font-size: 40px; font-weight: 700; margin: 16px 0 20px; }
.cta-box .btn {
  display: inline-block;
  font-size: 14px;
  font-weight: 700;
  background: var(--yellow);
  color: var(--ink);
  padding: 15px 30px;
  border-radius: var(--r);
  text-decoration: none;
}
.cta-box .btn:hover { opacity: .9; }
.cta-box .note { display: block; font-size: 12px; color: var(--ink3); margin-top: 14px; }

/* RODAPE */
#site-footer {
  background: #000;
  color: #fff;
  padding: 40px 0;
}
.footer-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 14px;
}
.footer-logo { font-size: 14px; font-weight: 700; color: #fff; text-decoration: none; }
.footer-link {
  font-size: 11px;
  color: #A0A0A0;
  text-decoration: none;
  letter-spacing: .05em;
  text-transform: uppercase;
}
.footer-link:hover { color: #fff; }
.footer-disc {
  font-size: 10px;
  color: #888;
  text-align: center;
  line-height: 1.7;
  margin-top: 20px;
}

@media (max-width: 600px) {
  .container, .narrow { padding: 0 22px; }
  .post-outer, .post-outer-container { padding: 22px; }
  .hero { padding: 60px 0 56px; }
}

  ]]></b:skin>
</head>

<body expr:class='data:blog.pageType'>

<!-- POPUP TEMPORARIO (site em desenvolvimento). Para remover, apague este bloco ate o comentario FIM POPUP TEMPORARIO. -->
<style>
  .dev-popup-overlay {
    position: fixed; inset: 0;
    background: rgba(26, 25, 23, 0.55);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    display: none;
    align-items: center; justify-content: center;
    z-index: 9999;
    padding: 24px;
    opacity: 0;
    transition: opacity .25s ease;
  }
  .dev-popup-overlay.is-open { display: flex; opacity: 1; }
  .dev-popup {
    background: #F5F3EE;
    color: #1A1917;
    max-width: 440px;
    width: 100%;
    border-radius: 8px;
    padding: 32px 28px 24px;
    box-shadow: 0 20px 60px rgba(0,0,0,.35);
    font-family: 'DM Sans', sans-serif;
    position: relative;
    transform: translateY(8px) scale(.98);
    transition: transform .25s ease;
  }
  .dev-popup-overlay.is-open .dev-popup { transform: translateY(0) scale(1); }
  .dev-popup__close {
    position: absolute; top: 10px; right: 12px;
    background: none; border: none; cursor: pointer;
    width: 32px; height: 32px;
    display: flex; align-items: center; justify-content: center;
    color: #6B6860; font-size: 22px; line-height: 1;
    border-radius: 4px;
  }
  .dev-popup__close:hover { background: #E8E5DE; color: #1A1917; }
  .dev-popup__tag {
    display: inline-block;
    background: #F5E642;
    color: #1A1917;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: .04em;
    text-transform: uppercase;
    padding: 4px 10px;
    border-radius: 3px;
    margin-bottom: 14px;
  }
  .dev-popup__title {
    font-size: 22px;
    font-weight: 600;
    line-height: 1.25;
    margin-bottom: 12px;
    color: #1A1917;
  }
  .dev-popup__text {
    font-size: 15px;
    line-height: 1.55;
    color: #3A3833;
    margin-bottom: 22px;
  }
  .dev-popup__text strong { color: #1A1917; }
  .dev-popup__actions { display: flex; flex-direction: column; gap: 10px; }
  .dev-popup__btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    padding: 13px 18px;
    border-radius: 6px;
    font-family: inherit;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    border: none;
    text-decoration: none;
    transition: transform .15s ease, background .15s ease;
  }
  .dev-popup__btn--primary { background: #25D366; color: #fff; }
  .dev-popup__btn--primary:hover { background: #1eb858; }
  .dev-popup__btn--secondary {
    background: transparent;
    color: #6B6860;
    border: 1px solid #C8C4BB;
  }
  .dev-popup__btn--secondary:hover { background: #E8E5DE; color: #1A1917; }
  .dev-popup__btn svg { width: 18px; height: 18px; }
  @media (max-width: 480px) {
    .dev-popup { padding: 28px 22px 20px; }
    .dev-popup__title { font-size: 19px; }
    .dev-popup__text  { font-size: 14px; }
  }
</style>

<div class='dev-popup-overlay' id='devPopup' role='dialog' aria-modal='true' aria-labelledby='devPopupTitle'>
  <div class='dev-popup'>
    <button class='dev-popup__close' id='devPopupClose' aria-label='Fechar' type='button'>&#215;</button>
    <span class='dev-popup__tag'>Aviso</span>
    <h2 class='dev-popup__title' id='devPopupTitle'>Site em ajustes finais</h2>
    <p class='dev-popup__text'>
      Estamos finalizando o checkout do site principal. Por enquanto, <strong>a compra do Setup é feita direto pelo WhatsApp</strong> — atendimento rápido e o mesmo preço de sempre.
    </p>
    <div class='dev-popup__actions'>
      <a href='#' class='dev-popup__btn dev-popup__btn--primary' id='devPopupWa'>
        <svg viewBox='0 0 24 24' fill='currentColor' aria-hidden='true'><path d='M19.05 4.91A10 10 0 0 0 2.46 17.5L1 23l5.65-1.48A10 10 0 1 0 19.05 4.91Zm-7.04 15.4h-.01a8.3 8.3 0 0 1-4.23-1.16l-.3-.18-3.36.88.9-3.27-.2-.33a8.32 8.32 0 1 1 7.2 4.06Zm4.56-6.22c-.25-.13-1.48-.73-1.71-.81-.23-.09-.4-.13-.56.13-.17.25-.65.81-.79.97-.15.17-.29.19-.54.06-.25-.13-1.06-.39-2.02-1.24-.74-.66-1.24-1.47-1.39-1.72-.15-.25-.02-.39.11-.51.11-.11.25-.29.37-.43.13-.15.17-.25.25-.42.08-.17.04-.31-.02-.43-.06-.13-.56-1.35-.77-1.84-.2-.49-.41-.42-.56-.43h-.48c-.17 0-.43.06-.66.31-.23.25-.86.84-.86 2.06s.88 2.39 1 2.55c.13.17 1.74 2.66 4.21 3.73.59.26 1.05.41 1.41.52.59.19 1.13.16 1.55.1.47-.07 1.48-.6 1.69-1.18.21-.59.21-1.08.15-1.18-.06-.11-.23-.17-.48-.3Z'/></svg>
        Comprar pelo WhatsApp
      </a>
      <button class='dev-popup__btn dev-popup__btn--secondary' id='devPopupContinue' type='button'>
        Continuar lendo o blog
      </button>
    </div>
  </div>
</div>

<script>
//<![CDATA[
(function(){
  var WA_NUMBER  = '5511915790207';
  var WA_MESSAGE = '*[Blog Setup Didi Index]*\n\nOlá! Tenho interesse em comprar o Setup do Didi Index. Pode me ajudar?';
  var KEY = 'devPopupSeen';

  var overlay     = document.getElementById('devPopup');
  var btnClose    = document.getElementById('devPopupClose');
  var btnContinue = document.getElementById('devPopupContinue');
  var btnWa       = document.getElementById('devPopupWa');

  btnWa.href = 'https://wa.me/' + WA_NUMBER + '?text=' + encodeURIComponent(WA_MESSAGE);

  function close(){
    overlay.classList.remove('is-open');
    try { sessionStorage.setItem(KEY, '1'); } catch(_) {}
    setTimeout(function(){ overlay.style.display = 'none'; }, 250);
  }
  function open(){
    overlay.style.display = 'flex';
    requestAnimationFrame(function(){ overlay.classList.add('is-open'); });
  }

  btnClose.addEventListener('click', close);
  btnContinue.addEventListener('click', close);
  btnWa.addEventListener('click', function(){
    try { sessionStorage.setItem(KEY, '1'); } catch(_) {}
  });
  overlay.addEventListener('click', function(e){
    if (e.target === overlay) close();
  });
  document.addEventListener('keydown', function(e){
    if (e.key === 'Escape' && overlay.classList.contains('is-open')) close();
  });

  var seen = false;
  try { seen = sessionStorage.getItem(KEY) === '1'; } catch(_) {}
  if (!seen) {
    setTimeout(open, 800);
  }
})();
//]]>
</script>
<!-- FIM POPUP TEMPORARIO -->

<div id='nav-bar'>
  <div class='container'>
    <a class='nav-logo' expr:href='data:blog.homepageUrl'>DIDI INDEX &#9658; SETUP</a>
    <div class='nav-right'>
      <a class='nav-link' expr:href='data:blog.homepageUrl'>Blog</a>
      <a class='nav-cta' href='https://bigplayer.vercel.app/'>Quero o Setup &#8594;</a>
    </div>
  </div>
</div>

<b:if cond='data:blog.pageType == &quot;index&quot;'>
  <div class='hero'>
    <div class='container'>
      <span class='hero-tag'>Blog &#183; Metodo Didi Index</span>
      <h1>Aprenda a operar com a <span class='hl'>Agulhada do Didi</span></h1>
      <p class='hero-sub'>
        Tutoriais e downloads do setup que os grandes operadores usam.
        Pronto para MetaTrader, Profit Chart e Tryd.
      </p>
      <div class='hero-cta'>
        <a class='btn-primary' href='https://bigplayer.vercel.app/'>Quero o Setup Pronto &#8594;</a>
      </div>
      <div class='hero-trust'>
        <span class='trust-item'><span class='trust-dot'></span> 4 plataformas inclusas</span>
        <span class='trust-item'><span class='trust-dot'></span> Download imediato</span>
        <span class='trust-item'><span class='trust-dot'></span> Curso completo gratis</span>
      </div>
    </div>
  </div>
<b:else/>
  <div class='page-band'>
    <div class='narrow'>
      <nav class='breadcrumb'>
        <a expr:href='data:blog.homepageUrl'>Inicio</a>
        <span class='bc-sep'>&#9656;</span>
        <a expr:href='data:blog.homepageUrl'>Blog</a>
        <span class='bc-sep'>&#9656;</span>
        <span class='bc-current'>Artigo</span>
      </nav>
    </div>
  </div>
</b:if>

<div class='main-outer'>
  <div class='container'>

    <b:if cond='data:blog.pageType == &quot;index&quot;'>
      <div class='sec-label'><span class='sec-num'>01</span> Artigos e tutoriais</div>
    </b:if>

    <b:section id='header' showaddelement='no'>
      <b:widget id='Header1' locked='false' title='Cabecalho' type='Header' version='2'/>
    </b:section>

    <b:section id='main' showaddelement='yes'>
      <b:widget id='Blog1' locked='true' title='Posts' type='Blog' version='2'/>
    </b:section>

  </div>
</div>

<b:if cond='data:blog.pageType == &quot;item&quot;'>
  <div class='related-band'>
    <div class='container'>
      <div class='sec-label'><span class='sec-num'>+</span> Continue lendo</div>
      <b:section id='related' showaddelement='no'>
        <b:widget id='PopularPosts1' locked='false' title='' type='PopularPosts' version='2'/>
      </b:section>
    </div>
  </div>
</b:if>

<div class='cta-band'>
  <div class='narrow'>
    <div class='cta-box'>
      <span class='kicker'>Setup completo</span>
      <h2>Didi Index pronto para MetaTrader, Profit Chart e Tryd</h2>
      <p>Bollinger, ADX, Didi Index, Estocastico e TRIX configurados.</p>
      <p>So copiar o arquivo e aplicar no grafico.</p>
      <div class='price'>R$ 29,90</div>
      <a class='btn' href='https://bigplayer.vercel.app/'>Quero o Setup &#8594;</a>
      <span class='note'>Entrega imediata por e-mail</span>
    </div>
  </div>
</div>

<div id='site-footer'>
  <div class='container'>
    <div class='footer-row'>
      <a class='footer-logo' expr:href='data:blog.homepageUrl'>DIDI INDEX &#9658; SETUP</a>
      <a class='footer-link' href='https://bigplayer.vercel.app/'>Ver o Setup</a>
    </div>
    <p class='footer-disc'>
      Este blog e exclusivamente educacional. Trading envolve risco de perda de capital.
      Resultados passados nao garantem resultados futuros.
    </p>
  </div>
</div>

</body>
</html>
